function operation(value, row, index)
{
    return "<a href='http://shenzc.blog.com//" + value + "' >替换图片</a>";
}

function check(value, row, index)
{
    return "<a href='http://shenzc.blog.com/MyFile/" + value + "' >文件下载</a>";
}
